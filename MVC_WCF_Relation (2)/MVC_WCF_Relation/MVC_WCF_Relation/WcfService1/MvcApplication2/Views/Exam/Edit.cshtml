@model MvcApplication2.ServiceReference1.exam

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>exam</legend>

        @Html.HiddenFor(model => model.examid)

        <div class="editor-label">
            @Html.LabelFor(model => model.per)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.per)
            @Html.ValidationMessageFor(model => model.per)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.rno)
        </div>
        <div class="editor-field">
           
            @Html.DropDownListFor(model => model.rno, (IEnumerable
        <SelectListItem>)ViewBag.city)
            @Html.ValidationMessageFor(model => model.rno)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.sem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.sem)
            @Html.ValidationMessageFor(model => model.sem)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

