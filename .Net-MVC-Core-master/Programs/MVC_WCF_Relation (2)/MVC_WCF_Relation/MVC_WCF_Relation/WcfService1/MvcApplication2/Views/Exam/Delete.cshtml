@model MvcApplication2.ServiceReference1.exam

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>exam</legend>

    <div class="display-label">per</div>
    <div class="display-field">@String.Format("{0:F}", Model.per)</div>

    <div class="display-label">rno</div>
    <div class="display-field">@String.Format("{0:F}", Model.rno)</div>

    <div class="display-label">sem</div>
    <div class="display-field">@String.Format("{0:F}", Model.sem)</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


